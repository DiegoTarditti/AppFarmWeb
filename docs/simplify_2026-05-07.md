# Simplify review — sesión 2026-05-07

Auditoría de duplicación / simplificaciones sobre los commits de hoy
(`e301441..b11b42c`). Solo análisis, nada tocado todavía.

---

## TL;DR

3 refactors concretos, total ~2h:

1. **[30min]** Migrar `compras_dia_armar.html` a `_grafico_dual_panel.html` → borra ~250 líneas.
2. **[1h]** Extraer `cargar_ventas_12m` + `clasificar_min` a `purchase_helpers.py` → elimina ~120 líneas en 4 callsites.
3. **[15min]** Limpiar imports dentro del loop en `compras_dia.py:625-649`.

Si solo hago uno: el **#1**, mejor ratio impacto/esfuerzo.

---

## 1. Duplicación grande — Forecast 12m + `analyze_product`

El bloque "calcular forecast por producto con `ObsVentaMensual` + `analyze_product`" aparece **4 veces casi idénticas** (~30 líneas cada uno):

| Archivo | Líneas | Contexto |
|---------|--------|----------|
| `routes/compras_dia.py` | 418-525 | route principal `/pedidos/dia/armar` |
| `routes/compras_dia.py` | 990-1030 | export XLSX correcciones de mínimos |
| `routes/compras_dia.py` | 1236-1284 | API buscar-producto |
| `routes/informes.py` | 550-586 | informe `/informes/correcciones-minimos` |

Patrón repetido en los 4:

```python
hoy = date.today()
end_month = hoy.month
start_month = ((end_month - 11 - 1) % 12) + 1
start_year = hoy.year if start_month <= end_month else hoy.year - 1
ventas_por_pid = {pid: [0]*12 for pid in obs_ids}
rows_vm = (session.query(...).group_by(...).all())
for pid_v, anio, mes, uds in rows_vm:
    offset = (anio - start_year) * 12 + (mes - start_month)
    if 0 <= offset <= 11 and pid_v in ventas_por_pid:
        ventas_por_pid[pid_v][offset] += int(uds or 0)
```

Y el bloque de **clasificación de mínimo** aparece **literal 4 veces**:

```python
if min_actual == 0:
    min_sugerencia = 'up' if min_sugerido > 0 else 'ok'
elif min_actual < min_sugerido * 0.6:
    min_sugerencia = 'up'
elif min_actual > min_sugerido * 2.0:
    min_sugerencia = 'down'
else:
    min_sugerencia = 'ok'
```

### Helper sugerido

Crear `purchase_helpers.py` (al lado de `purchase_engine.py`):

```python
def cargar_ventas_12m(session, pids: list[int]) -> tuple[dict[int, list[int]], int, int]:
    """Devuelve (ventas_por_pid, start_month, start_year) — array 12m por pid."""

def calcular_min_sugerido(ventas: list[int], stock: int, *, dias_target: int = 30) -> tuple[int, bool]:
    """Devuelve (min_sugerido, sin_movimiento)."""

def clasificar_min(min_actual: int, min_sugerido: int) -> str:
    """Devuelve 'up' | 'down' | 'ok'."""
```

**Beneficio**: cualquier ajuste futuro (umbrales 0.6/2.0, ventana 12m → 6m, agregar prorrateo del mes corriente) se hace en un solo lugar.

**Riesgo**: bajo, los 4 callsites son idénticos modulo nombres de variables.

---

## 2. Duplicación grande — Panel chart dual

`templates/_grafico_dual_panel.html` se creó hoy precisamente para deduplicar el chart de Año + Mes. **Pero `compras_dia_armar.html` no lo adoptó** — sigue teniendo el bloque inline.

| Archivo | Líneas | |
|---------|--------|--|
| `templates/_grafico_dual_panel.html` | 46-281 | Componente nuevo |
| `templates/compras_dia_armar.html` | 540-792 | Bloque inline ~equivalente |

Ambos tienen `renderAnio` / `renderMes` / `cargarChartAnio` / `cargarChartMes` con la misma lógica. Solo difieren en los IDs de los canvas (`dual-chart-canvas-anio` vs `chart-canvas-anio`) y en cómo se conectan a la lista de productos.

### Migración

En `compras_dia_armar.html`:

```jinja
{% include '_grafico_dual_panel.html' %}
```

Y en JS: reemplazar el bloque inline por llamadas a `window.cargarGraficoDual(observerId, nombre)` cuando cambia la fila seleccionada.

**Cuidado**: verificar que la sincronización con los filtros de la página (rubro, drog) siga funcionando. El componente dual no debería conocer esos filtros — la página le pasa el observer_id ya resuelto.

**Esfuerzo**: 30min + smoke test.

---

## 3. Simplificaciones locales (cherry-picks)

### 3.1 Imports dentro del loop — `routes/compras_dia.py:625-649`

```python
for r in base:
    from purchase_engine import (
        analyze_product, FULL_MONTHS, _prorate_partial,
        start_month_idx_from_period, tipo_producto,
    )
    import math
    ...
```

Se ejecuta en cada iteración (Python cachea en `sys.modules`, no es caro pero ensucia). Subir al top del `with get_db()`.

### 3.2 `import math as _math` redundante — `routes/compras_dia.py:1257`

Ya hay otros `import math` en el módulo. Usar uno solo top-level.

### 3.3 Sufijos numerados en imports

Aparecen `_date2`, `_td2`, `_datef`, `_dt_d` en el mismo módulo. Top-level una vez:

```python
from datetime import date, datetime, timedelta
```

Y reusar.

### 3.4 Parseo de `rubros_raw` duplicado

```python
rubros_raw = (request.args.get('rubros') or '12').strip()
if rubros_raw.lower() == 'all' or not rubros_raw:
    rubros_filtro = None
else:
    try: rubros_filtro = {int(x) for x in rubros_raw.split(',')}
    except: rubros_filtro = {12}
```

Idéntico en `routes/compras_dia.py:366-373` y `routes/informes.py:498-505`. Helper de 5 líneas en `helpers.py`:

```python
def parse_rubros_filter(raw: str | None, default: set[int] = {12}) -> set[int] | None:
    """'12,5' → {12,5}, 'all' → None, vacío → default."""
```

---

## 4. Bugs sutiles (anotar, no urgente)

### 4.1 `producto_matcher.py:769` — dedup fallback

Cuando un candidato no tiene ni `codigo_alfabeta` ni `codigo_barra`, la key cae a `(_origen, id)`. El `_origen` se setea con `setdefault('global')` en línea 728. Si dos pools custom pasaran candidatos sin alfa/EAN con mismo id, podrían colapsar.

Bajo riesgo en uso actual (siempre hay EAN o alfa en candidatos reales). Anotable, no bloquea.

### 4.2 `routes/providers.py` — `provider_create_manual`

Para detectar duplicados, itera **todos** los providers con `_normalizar_nombre_entidad` en Python. N² si la tabla crece a miles. Hoy son decenas, no urge. Solución futura: índice único en columna calculada de nombre normalizado.

---

## 5. Sobre-ingeniería detectada

Ninguna grave en este rango. El componente `_grafico_dual_panel.html` está bien diseñado — solo falta que el callsite original lo adopte (ver #2).

---

## 6. Plan de ejecución sugerido

### Sesión corta (30min) — solo lo crítico
- Migrar `compras_dia_armar.html` a `{% include '_grafico_dual_panel.html' %}` (#1 del top 3).

### Sesión normal (~2h) — los 3 refactors
1. Migrar template (30min, #1).
2. Crear `purchase_helpers.py`, extraer 3 helpers, reemplazar 4 callsites (1h, #2).
3. Limpiar imports dentro del loop + import math redundante + sufijos `_date2/_td2` (15min, #3).
4. Smoke test: abrir `/compras/armar`, `/informes/correcciones-minimos`, export XLSX (15min).

### Sesión larga (~3h) — refactor + helpers chicos
- Lo de arriba + extraer `parse_rubros_filter` (3.4) + revisar dedup matcher (4.1).

---

## Notas

- **No tocar nada antes de probar lo de hoy** (`docs/pendiente_de_prueba_2026-05-07.md`). Estos refactors no agregan features, solo simplifican; pueden esperar a que validemos que lo de hoy funciona en prod.
- Si algo del refactor del helper rompe el cálculo de min_sugerido, los reverts del doc de pendientes siguen sirviendo.
- Después del refactor del template (#1), revisar que el chart abra en menos tiempo (menos JS parseado).
